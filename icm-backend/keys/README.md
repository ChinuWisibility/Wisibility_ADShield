# Public keys for offline license verification (LMS-compatible layout).
#
# Preferred:  keys/{kid}/public.pem     e.g. keys/key-2026-01/public.pem
# Also accepted: keys/{kid}.pub         e.g. keys/key-2026-01.pub
# Legacy:     keys/public.pem
#
# Never store private keys in the Identity Sphere deployment package.
